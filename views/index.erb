<html>
  <head>
    <title>WebGuesser</title>
    <style>
      #way_off { color: Crimson; }
      #off { color: Tomato; }
      #correct { color: Green; }
      #guess { width: 75px; }
      #guess:focus { outline: none }
    </style>
  </head>
  <body>
    <h1>WebGuesser</h1>
    <p>
      <p><%= settings.intro %></p>
      <form>
        Your guess: <input type='text' name='guess' id='guess' autofocus>
        <input type='submit' value='Guess!'>
      </form>

      <% if settings.message != nil %>
        <p><%= settings.message %></p>
      <% end %>

      <% if settings.show_number %>
        <p>The secret number is <%= settings.number %>.</p>
      <% end %>

      <% if settings.play_again != nil %>
        <p><%= settings.play_again %></p>
      <% end %>
    </p>
  </body>
</html>
